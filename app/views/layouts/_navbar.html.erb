<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">EvenbriteV2</a>
    </div>
    <ul class="nav navbar-nav">
      <li><a href="/">Home</a></li>
      <% if user_signed_in? %>
        <li><a href="events/new">Create event</a></li>
        <%else%>
      <% end %>

      <li><a href="<%= events_path %>">All Events</a></li>
      <li><a href="<%= attended_path %>">Events attended</a></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
      <li></li>
      <% else %>
      <li><a href="<%= new_user_registration_path %>"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
      <% end %>
        <% if user_signed_in? %>
      <li style="color: white; display: inline;"> <%= current_user.email %></li>
      <li style="color: white; display: inline;"><span  class="glyphicon glyphicon-log-in"></span> <%= link_to('Logout', destroy_user_session_path, method: :delete) %></li>
      <% else %>
      <li><a href="<%= new_user_session_path %>"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
      <%end%>
    </ul>
  </div>
</nav>
